To run this program:

1.  First type 'make' to compile using the makefile (ignore the gcc warnings)
2.  Type 'smallsh' to run the program
3.  Type 'p3testscript smallsh' to run the program with the grading script
